[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name = "flanker-ai"
version = "0.1.0"
description = "AI agent for Project Flanker"
requires-python = ">=3.13"