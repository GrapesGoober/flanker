[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flanker_ai", "src/flanker_core"]

[project]
name = "flanker-backend"
version = "0.1.0"
description = "Project Flanker backend library logic."
requires-python = ">=3.13"